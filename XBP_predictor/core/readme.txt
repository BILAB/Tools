class dataset:
   dataset.ids: list of pdbid + chain ID
   (ex:
       ['148lE','3dxgB']
   dataset.test(ids): return test dataset ( ids is use the test proteins
   dataset.train(ids): return train dataset ( ids is use the train proteins